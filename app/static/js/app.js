// You can add additional Petite-Vue configurations here if needed
import PetiteVue from "../../../.venv/Lib/site-packages/nicegui/static/vue.global";

document.addEventListener("DOMContentLoaded", () => {
    PetiteVue.createApp({}).mount();
});